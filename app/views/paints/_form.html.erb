<%= form_with(model: paint, local: true) do |form| %>
  <% if paint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(paint.errors.count, "error") %> prohibited this paint from being saved:</h2>

      <ul>
      <% paint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :paint_title %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category, id: :paint_category %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :paint_year %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :paint_description %>
  </div>

  <div class="field">
    <%= form.label :comments %>
    <%= form.text_area :comments, id: :paint_comments %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :paint_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
